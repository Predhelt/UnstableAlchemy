class_name CrawlSpace extends StaticBody2D

## The size of the gap in the crawlspace.
## If the character's size is less than or equal to the crawlspace size, they can pass through it.
@export var gap_size : float = 100.0
